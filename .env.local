# Copy this file to .env.local and set your own values before deploying.
# Both fall back to insecure defaults ("changeme" / a fixed dev string) if unset —
# fine for local testing, NOT fine for a public deployment.

ADMIN_PASSWORD=Cor9s632
ADMIN_SECRET=replace-with-a-long-random-string
